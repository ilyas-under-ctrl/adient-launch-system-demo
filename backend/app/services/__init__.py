# Business logic that's more than plain CRUD goes here (one file per
# domain, e.g. simulation_service.py, launch_service.py), keeping routers
# thin (just request/response handling) and models thin (just data shape).
